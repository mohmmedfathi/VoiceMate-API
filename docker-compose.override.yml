services:
  web:
    volumes:
      - .:/app
      - data:/app/data
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  worker:
    volumes:
      - .:/app
      - data:/app/data
      - whisper_cache:/root/.cache/whisper
